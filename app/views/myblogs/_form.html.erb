<%= form_for(@myblog) do |f| %>
  <% if @myblog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@myblog.errors.count, "error") %> prohibited this myblog from being saved:</h2>

      <ul>
      <% @myblog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
